import sys

def eatingout(lines):
    nums = [int(num) for num in lines[0].split(" ")]
    m = nums[0]
    a = nums[1]
    b = nums[2]
    c = nums[3]
    total_picked = a

    if (a - b < 0):
        total_picked += -(a - b)

    if (total_picked)


    if :
        return "possible"
    else:
        return "impossible"


def main():
    lines = [line.strip() for line in sys.stdin]
    print(eatingout(lines))
main()
